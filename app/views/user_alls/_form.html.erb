<%= form_with(model: user_all) do |form| %>
  <div class="modal-body">
    <% if user_all.errors.any? %>
      <div style="color: red">
        <h2><%= pluralize(user_all.errors.count, "error") %> prohibited this user_all from being saved:</h2>

        <ul>
          <% user_all.errors.each do |error| %>
            <li><%= error.full_message %></li>
          <% end %>
        </ul>
      </div>
    <% end %>

    <div class="form-group">
      <%= form.label :name, class: "form-label mb-2 mt-3" %>
      <%= form.text_field :name, class: "form-control form-control-lg" %>
    </div>

    <div class="form-group">
      <%= form.label :last_name, class: "form-label mb-2 mt-3" %>
      <%= form.text_field :last_name, class: "form-control form-control-lg" %>
    </div>

    <div class="form-group">
      <%= form.label :address, class: "form-label mb-2 mt-3" %>
      <%= form.text_field :address, class: "form-control form-control-lg" %>
    </div>

    <div class="form-group">
      <%= form.label :contact_no, class: "form-label mb-2 mt-3" %>
      <%= form.text_field :contact_no, class: "form-control form-control-lg" %>
    </div>

    <div class="form-group">
      <%= form.label :city, class: "form-label mb-2 mt-3" %>
      <%= form.text_field :city, class: "form-control form-control-lg" %>
    </div>

    <div class="form-group">
      <%= form.label :state, class: "form-label mb-2 mt-3" %>
      <%= form.text_field :state, class: "form-control form-control-lg" %>
    </div>
  </div>
  <div class="modal-footer">
    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>
    <%= form.submit class: "btn btn-primary", data: {action: "click->bs-modal#submitEnd"} %>
  </div>

<% end %>
